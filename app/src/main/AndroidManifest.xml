<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.postsuicide5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Stats"></activity>
        <activity android:name=".FifthQuestion" />
        <activity android:name=".FourthQuestion" />
        <activity android:name=".ThirdActivity" />
        <activity android:name=".SecQuestion" />
        <activity android:name=".FinalStats" />
        <activity android:name=".FirstQuestion" />
        <activity android:name=".WeeklyLogInstruc" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>